# Web Prototype Reference Status

The root web project is retained solely as a **reference prototype**. It demonstrates early interaction ideas, documentation, visual experiments, and browser validation.

It is **not** the NeoRumaz production target, not an Android build, not a Unity project, and not evidence of advertisement integration. All future product implementation work belongs in `Unity/NeoRumaz/`.
